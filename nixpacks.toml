[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.build]
cmds = ["pnpm --filter @workspace/portfolio build && pnpm --filter @workspace/api-server build"]

[start]
cmd = "node artifacts/api-server/dist/index.mjs"
